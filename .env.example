# This is a combined environment file example for Render deployment
# Copy these to your Render environment variables

# === Backend Configuration ===
NODE_ENV=production
PORT=10000

# === MongoDB Configuration ===
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/email-leads

# === IMAP Email Configuration ===
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_SECURE=true
EMAIL_USER=your.email@domain.com
EMAIL_PASS=your-app-specific-password

# === Email Filter Configuration ===
SENDER_EMAIL=sender@domain.com
SUBJECT_PATTERN=Your Subject Pattern

# === Frontend/CORS Configuration ===
FRONTEND_URL=https://your-app-name.onrender.com

# === Frontend API URL (for development only) ===
# VITE_API_URL=http://localhost:5000
